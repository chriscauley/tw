server {
    server_name timewalker.io tw.tfibhodc.com;

    #location /static/ {
    #    root /home/chriscauley/projects/ur-conf/._staticfiles/;
    #}

    location / {
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   Host      $http_host;
        proxy_pass         http://127.0.0.1:8089;
    }
}
